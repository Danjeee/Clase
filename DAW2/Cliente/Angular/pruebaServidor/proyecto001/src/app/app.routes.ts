import { Routes } from '@angular/router';
import { ArticulosComponent } from './components/articulos/articulos.components';

export const routes: Routes = [
    { path: '', component: ArticulosComponent },
];
